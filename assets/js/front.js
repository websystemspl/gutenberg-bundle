/**
 * Front-end entry: bundles the stylesheets required to display saved core blocks.
 * The generated front.js itself carries no behaviour and does not need to be loaded.
 */
import '../css/front.scss';
